import assets from "./assets";
import { COLORS, SHADOWS, SIZES, FONTS } from "./theme";
import { NFTData, GorillaData } from "./dummy";
import {
  SEARCH_THRESHOLD,
  MAX_CANDIDATES,
  VERIFICATION_THRESHOLD,
} from "./inference";

import { FormStyles } from "./formStyles";

export {
  assets,
  COLORS,
  SHADOWS,
  SIZES,
  FONTS,
  NFTData,
  GorillaData,
  SEARCH_THRESHOLD,
  MAX_CANDIDATES,
  VERIFICATION_THRESHOLD,
  FormStyles,
};
